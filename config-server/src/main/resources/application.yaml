server:
  port: 8081
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hughob36/config-server-test.git
          search-paths: config-data
          username: ${GIT_USERNAME}
          password: ${GIT_PASSWORD}